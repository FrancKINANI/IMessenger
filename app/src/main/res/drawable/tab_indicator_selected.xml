<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" />
    <corners android:radius="20dp" />
    <stroke android:width="0dp" android:color="@android:color/transparent" />
    <padding android:left="0dp" android:top="0dp" android:right="0dp" android:bottom="0dp" />
    <!-- Actually, for a selected tab indicator, maybe just a bottom border or a pill shape. 
         Let's make it a pill shape with white background and blue text (handled in code or via selector).
         For now, this is a simple white pill background. -->
    <solid android:color="#33FFFFFF" /> 
</shape>
